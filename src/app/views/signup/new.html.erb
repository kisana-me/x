<% content_for :title, "得句巣乃使用開始" %>

<h1>得句巣乃使用開始</h1>
<p>貴方以下規約同意必須</p>
<p>使用開始後、貴方規約同意済認識</p>
<p><small>※利用開始すると利用規約及びプライバシーポリシーに同意したとみなされます。</small></p>

<% if session[:oauth_signup] %>
  <p style="color: #ff5000;">ANYUR連携中</p>
<% end %>

<%= form_with(model: @account, url: signup_path, method: :post, class: "ctmf", data: {turbo: false}) do |form| %>
  <%= render "shared/error_messages", instead: @account %>
  <div>
    <%= form.label :name %> <small>漢字</small><br />
    <%= form.text_field :name, value: session[:oauth_signup]&.dig("name") || "無名" %>
  </div>
  <div>
    <%= form.label :name_id %> <small>英数字</small><br />
    <%= form.text_field :name_id, placeholder: "省略可", value: session[:oauth_signup]&.dig("name_id") %>
  </div>
  <div>
    <%= form.submit "開始" %>
  </div>
<% end %>
