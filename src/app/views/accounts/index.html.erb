<% content_for :title, "口座一覧" %>

<% @accounts.each do |account| %>
  <div>
    名前:
    <%= account.name %>
  </div>
  <div>
    識別子:
    <%= link_to account.name_id, account_path(account.name_id) %>
  </div>
  <hr />
<% end %>
