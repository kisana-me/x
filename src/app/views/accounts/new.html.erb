<% content_for :title, "得句巣乃使用開始" %>

<h1>得句巣乃使用開始</h1>
<p>貴方以下規約同意必須</p>
<p>使用開始後、貴方規約同意済認識</p>
<small>※アカウントを作成すると利用規約及びプライバシーポリシーに同意したとみなされます。</small>

<h2>新規口座作成</h2>

<%= form_with(model: @account) do |form| %>
  <%= render "shared/error_messages", instead: @account %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "作成" %>
  </div>
<% end %>

<h2>既口座作成済</h2>

<%= form_with(url: login_path, method: :post, local: true) do |form| %>
  <% if @login_error %>
    <div style="color: red">
      <%= @login_error %>
    </div>
  <% end %>
  <div>
    <%= form.label :login_password, I18n.t('activerecord.attributes.application.login_password'), style: "display: block" %>
    <%= form.text_field :login_password, value: params[:login_password] %>
  </div>

  <div>
    <%= form.submit "進入" %>
  </div>
<% end %>